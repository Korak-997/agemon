# no-agents-installed fixture

A repository with no agent rule files, no structured configs, and no Copilot
instructions — every adapter's `discoverProjectResources` should report
`exists: false` for every resource here.
