# claude-code-only fixture

A repository configured for Claude Code only: `CLAUDE.md` and
`.claude/settings.json` are present. No `GEMINI.md`, `.gemini/settings.json`,
or Copilot instruction files exist, so only the Claude Code adapter should
report `configured: true`.
