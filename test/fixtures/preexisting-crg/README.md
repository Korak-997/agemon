# preexisting-crg fixture

Used by the sandbox harness to simulate a repository where code-review-graph is already installed before agemon runs.

The harness sets AGEMON_FAKE_PREINSTALLED_CRG=1 for this fixture in fake mode.
